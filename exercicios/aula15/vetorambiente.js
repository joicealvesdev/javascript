let valores = [8 ,1 , 4, 5, 7]

/*for(let pos =0; pos<valores.length; pos++) {
    console.log(`A posição${pos} tem o valor ${valores[pos]}`)}  forma tradicional*/

for(let pos in valores) {
    console.log(`a posição ${pos} tem o valor ${valores[pos]}`) } /*forma simples*/
        
        
